<header class="w-full bg-linear-to-r from-green-600 to-green-500 text-white shadow-md"
        data-controller="menu">
  <div class="container mx-auto flex items-center justify-between px-6 py-4">

    <%= link_to root_path, class: "flex items-center gap-2 text-xl font-bold tracking-wide hover:opacity-90 transition" do %>
      <span>🥕</span>
      <span>HIGASHIYAMA FARM</span>
    <% end %>

    <!-- PCメニュー -->
    <nav class="hidden md:flex gap-6 text-sm font-medium">
      <%= link_to "予約する", new_reservation_path, class: "hover:underline" %>
      <%= link_to "予約確認", search_reservations_path, class: "hover:underline" %>
    </nav>

    <!-- モバイルハンバーガー -->
    <button type="button"
            class="md:hidden p-3 rounded-md hover:bg-white/10 transition"
            data-action="menu#open">

      <svg xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke-width="2"
          stroke="currentColor"
          class="h-6 w-6 stroke-[2.5]">
        <path stroke-linecap="round"
              stroke-linejoin="round"
              d="M3 6h18M3 12h18M3 18h18" />
      </svg>
    </button>

  <!-- オーバーレイ -->
  <div class="fixed inset-0 bg-black/30 hidden md:hidden"
      data-menu-target="overlay"
      data-action="click->menu#close"></div>

  <!-- モバイルスライドメニュー -->
  <div class="fixed top-0 right-0 h-full w-64 bg-white text-gray-800 shadow-lg transform translate-x-full transition-transform duration-300 md:hidden"
      data-menu-target="panel">

    <div class="flex justify-between items-center p-4 border-b">
      <span class="font-bold text-green-600">MENU</span>
      <button data-action="menu#close">✕</button>
    </div>

    <nav class="flex flex-col gap-4 p-6 text-sm">
      <%= link_to "予約する", new_reservation_path, class: "hover:text-green-600", data: { action: "menu#close" } %>
      <%= link_to "予約確認", search_reservations_path, class: "hover:text-green-600", data: { action: "menu#close" } %>
    </nav>
  </div>
</header>